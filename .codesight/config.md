# Config

## Environment Variables

- `MCP_AUTH_TOKEN` **required** — .env.example
- `MCP_SERVER_NAME` (has default) — .env.example
- `MCP_SERVER_URL` (has default) — .env.example
- `MCP_TIMEOUT` (has default) — .env.example
- `MCP_TRANSPORT` (has default) — .env.example
- `MLFLOW_EXPERIMENT_NAME` (has default) — .env.example
- `MLFLOW_TRACKING_URI` (has default) — .env.example
- `MONGODB_DB` (has default) — .env.example
- `MONGODB_URI` (has default) — .env.example
- `OPENAI_API_KEY` (has default) — .env.example
- `OPENAI_BASE_URL` **required** — baseagent\base_agent.py
- `OPENAI_MODEL` (has default) — .env.example

## Config Files

- `.env.example`
